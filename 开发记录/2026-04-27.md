# 2026-04-27 开发记录

## 1. 建立流程文档

### 本次目标

先把自定义刷题网站的开发流程写清楚，后续开发可以按文档推进。

### 已完成

- 创建项目流程文档。
- 明确第一阶段只做 Java 刷题。
- 梳理用户、题目、代码编辑、判题、提交记录等核心模块。
- 写出数据模型、API 草案、页面流程和开发里程碑。

### 修改文件

- `刷题网站流程文档.md`

### 验证结果

- 文档已创建在项目根目录。
- 后续开发可以参考该文档。

## 2. 建立最小可用版本

### 本次目标

先做一个可以真正运行的本地 MVP，重点打通“自定义题目 -> 写 Java -> 运行样例 -> 提交判题”。

### 已完成

- 创建无第三方依赖的 Node.js 后端。
- 创建静态前端页面。
- 创建本地 JSON 数据文件。
- 内置一个“两数之和”示例题。
- 支持题目列表和题目详情。
- 支持新建、编辑、删除自定义题目。
- 支持配置样例用例和隐藏用例。
- 支持在网页里写 Java 代码。
- 支持运行样例。
- 支持正式提交并使用全部测试用例判题。
- 支持基础 Java 代码检查、编译超时、运行超时和输出限制。

### 修改文件

- `package.json`
- `.gitignore`
- `server.js`
- `data/db.json`
- `public/index.html`
- `public/styles.css`
- `public/app.js`

### 验证结果

- `node --check server.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`。

## 13. 固定整体页面滚动并新增题目解析标签

### 本次目标

继续优化前端 UI：页面整体不再跟随浏览器滚动，只让页面内部区域滚动；去掉右侧可能出现的双滚动条；在提交记录旁边新增“题目解析”标签。

### 已完成

- 将 `html`、`body` 和应用根容器设置为固定高度，桌面端隐藏浏览器级滚动条。
- 左侧题目列表改为内部滚动。
- 中间题目卡片改为内部滚动，题目描述、提交记录、题目解析都在卡片内部切换和滚动。
- 代码编辑区和运行结果区改为面板内部布局，避免撑开整个页面。
- 右侧题目管理只保留表单内部滚动，避免最右侧出现两个滚动条。
- 在 `题目描述`、`提交记录` 旁边新增 `题目解析` 标签。
- 题目解析当前先展示占位内容，后续可以扩展为手写解析字段或大模型生成解析。
- 更新 `README.md` 的页面说明。

### 修改文件

- `public/index.html`
- `public/styles.css`
- `public/app.js`
- `README.md`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check public/app.js` 通过。
- `node --check server.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`。
- `data/db.json` 当前仍有本地运行记录变化，本次不会提交该文件。
- 示例题 Java 模板正式提交结果为 `ACCEPTED`。
- 示例题提交通过 `3/3` 个测试用例。
- 临时自定义题目验证通过：新建题目、运行样例、删除题目都正常。

### 当前限制

- 这是本地开发版，不是正式线上版。
- Java 代码目前在本机进程中运行。
- 后续需要迁移到 Docker 隔离判题，安全性才适合正式使用。

## 3. 补充新手说明

### 本次目标

把项目说明写得更适合初学者，让你能照着步骤启动和使用。

### 已完成

- 重写 `README.md`。
- 说明启动前如何检查 Node.js 和 Java。
- 说明如何进入项目目录。
- 说明如何启动网站。
- 说明如何查看实际访问地址。
- 说明如何停止网站。
- 说明页面左侧、中间、右侧分别怎么用。
- 说明新建题目的详细步骤。
- 说明 Java 代码要求。
- 说明常见判题结果的含义。
- 说明数据保存位置和当前安全限制。

### 修改文件

- `README.md`

### 验证结果

- `README.md` 已创建并可读取。
- 当前网站仍可访问，首页返回 `200`。

## 4. 新增开发记录机制

### 本次目标

建立专门的开发记录目录，以后每次开发都记录变更内容。

### 已完成

- 创建 `开发记录/` 目录。
- 创建开发记录说明文件。
- 补记今天已经完成的开发工作。

### 修改文件

- `开发记录/README.md`
- `开发记录/2026-04-27.md`

### 验证结果

- 记录目录已建立。
- 后续每次开发都可以继续在这里追加记录。

## 5. 优化中文文档查看体验

### 本次目标

避免在 Windows PowerShell 中直接查看中文 Markdown 时出现乱码，降低新手查看文档的难度。

### 已完成

- 将主要中文 Markdown 文件统一改为 Windows 更容易识别的 UTF-8 BOM 编码。
- 确认 PowerShell 中可以正常显示开发记录文件的中文内容。

### 修改文件

- `README.md`
- `刷题网站流程文档.md`
- `开发记录/README.md`
- `开发记录/2026-04-27.md`

### 验证结果

- 使用 `Get-Content -LiteralPath "开发记录\2026-04-27.md" -TotalCount 12` 查看时，中文显示正常。

## 6. 后端代码补充解释注释

### 本次目标

继续完善后端代码，让初学者阅读 `server.js` 时能理解主要流程，不只是看到代码本身。

### 已完成

- 在后端路径配置位置补充注释，解释各目录和限制常量的作用。
- 在服务入口位置补充注释，解释浏览器请求如何进入后端。
- 在 API 分发位置补充注释，解释题目接口、提交接口和静态文件请求的区别。
- 在提交创建流程补充注释，解释“运行样例”和“正式提交”的区别。
- 在 Java 判题流程补充注释，解释编译、运行、输出比较和临时目录清理。
- 在数据读写、请求体读取、错误返回等工具函数处补充注释。
- 将 `server.js` 调整为 UTF-8 BOM 编码，方便 Windows PowerShell 直接查看中文注释。

### 修改文件

- `server.js`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check server.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`。
- 使用临时题目验证正式提交，结果为 `ACCEPTED`。
- 临时题目通过 `2/2` 个测试用例。
- 验证后临时题目和临时提交已删除，没有留下测试数据。

### 额外说明

- 当前 `data/db.json` 中题目列表为空，可能是你在页面上删除了示例题。
- 我没有自动恢复示例题，避免覆盖你的实际操作。

## 7. 准备 GitHub 推送并恢复示例题

### 本次目标

最小可用版本开发完成后，准备推送到 GitHub，同时按要求不上传开发记录目录和流程文档，并恢复“两数之和”示例题。

### 已完成

- 恢复 `data/db.json` 中的“两数之和”示例题。
- 清空提交记录，避免上传本地测试提交。
- 更新 `.gitignore`，排除 `开发记录/`。
- 更新 `.gitignore`，排除 `刷题网站流程文档.md`。
- 初始化本地 Git 仓库，默认分支为 `main`。
- 使用 `git check-ignore` 确认开发记录和流程文档会被 Git 忽略。
- 创建本地 Git 提交：`cc1f266 Initial CodingWeb MVP`。

### 修改文件

- `.gitignore`
- `data/db.json`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check server.js` 通过。
- 题目接口可以看到恢复后的“两数之和”。
- `git check-ignore -v "开发记录/2026-04-27.md" "刷题网站流程文档.md"` 确认两个路径都被忽略。
- `git commit -m "Initial CodingWeb MVP"` 执行成功。

### 后续需要

- 已收到 GitHub 仓库地址：`https://github.com/congxxx/CodingWeb.git`。
- 已添加远程仓库 `origin`。
- 已推送 `main` 分支到 GitHub。
- 本地 `main` 已设置为跟踪 `origin/main`。

### 推送结果

- 远程仓库：`https://github.com/congxxx/CodingWeb.git`
- 推送分支：`main`
- 推送提交：`cc1f266 Initial CodingWeb MVP`
- `git status --short --branch` 显示：`## main...origin/main`

## 8. 记录 AI 题目整理后续需求

### 本次目标

先把“调用大模型 API 整理题目，并按固定格式返回后保存到自己的题目中”这个需求加入流程文档，作为后续开发规划。

### 已完成

- 在第一阶段范围中说明 AI 辅助写题当前 MVP 暂不支持，但作为后续规划。
- 新增 `5.6 AI 题目整理模块`。
- 补充 AI 题目整理的目标、适用场景、建议能力和页面流程。
- 补充大模型返回 JSON 的推荐格式。
- 补充后端处理要求，强调 API Key 只能放后端环境变量。
- 补充 AI 题目整理接口草案。
- 补充管理员使用 AI 整理题目的页面流程。
- 新增 `里程碑 6：AI 题目整理`。
- 补充 AI 题目整理的额外风险和应对方式。

### 修改文件

- `刷题网站流程文档.md`
- `开发记录/2026-04-27.md`

### 验证结果

- 使用 `rg -n "AI 题目整理|大模型|normalize|里程碑 6" "刷题网站流程文档.md"` 确认新增内容已写入。
- 使用 `git status --short --ignored` 确认 `刷题网站流程文档.md` 仍然被 Git 忽略，不会上传到 GitHub。

### 后续建议

- 真正开发该功能前，需要确定使用哪个大模型服务商。
- 需要先设计后端环境变量名称，例如 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`。
- 需要设计模型返回 JSON 的校验逻辑，避免格式错误或字段缺失。

## 9. 新增提交记录查看功能

### 本次目标

补齐页面上的提交记录查看能力，让用户不仅能运行和提交，还能回看历史运行/提交结果。

### 已完成

- 在做题区域下方新增“提交记录”区域。
- 支持显示最近的样例运行和正式提交。
- 每条记录显示题目名、判题状态、通过用例数量、耗时和提交时间。
- 新增提交详情弹窗。
- 详情弹窗中展示判题结果、用例结果、错误信息和提交代码。
- 运行样例或正式提交后，提交记录会自动刷新。
- 删除题目后，提交记录会同步刷新。
- 将前端 HTML、CSS、JS 文件调整为 UTF-8 BOM 编码，方便 Windows PowerShell 直接查看中文。
- 更新 `README.md`，补充提交记录和提交详情的使用说明。

### 修改文件

- `public/index.html`
- `public/styles.css`
- `public/app.js`
- `README.md`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check public/app.js` 通过。
- `node --check server.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`。
- 页面 HTML 中可以找到“提交记录”和 `submissionModal`。
- 使用示例题提交一次，结果为 `ACCEPTED`，通过 `3/3` 个用例。
- `/api/submissions` 能返回提交记录列表。
- `/api/submissions/{id}` 能返回提交详情和代码。
- 验证完成后已清空测试提交记录，避免污染初始数据。
- `README.md` 中已能检索到“提交记录”和“提交详情”说明。

### Git 提交

- 本地提交：`e73d140 Add submission history viewer`
- 已推送到远程仓库：`https://github.com/congxxx/CodingWeb.git`
- `git status --short --branch` 显示：`## main...origin/main`

## 10. 新增恢复示例题功能

### 本次目标

增加“恢复示例题”能力，避免你不小心删除“两数之和”后需要手动修改 `data/db.json`。

### 已完成

- 后端新增 `POST /api/problems/seed` 接口。
- 前端右上角新增“恢复示例题”按钮。
- 如果示例题不存在，点击按钮会重新创建“两数之和”。
- 如果示例题已经存在，点击按钮不会重复创建。
- 将示例题 ID 固定为 `seed-two-sum`。
- 将示例题测试用例 ID 固定为 `seed-case-1`、`seed-case-2`、`seed-case-3`。
- 更新 `README.md`，补充恢复示例题的使用说明。

### 修改文件

- `server.js`
- `public/index.html`
- `public/app.js`
- `README.md`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check server.js` 通过。
- `node --check public/app.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`，页面包含“恢复示例题”。
- 删除 `seed-two-sum` 后，调用 `POST /api/problems/seed` 可以恢复示例题。
- 第一次恢复返回 `restored: true`。
- 第二次恢复返回 `restored: false`，不会重复创建。
- 恢复后的示例题提交结果为 `ACCEPTED`，通过 `3/3` 个用例。
- 验证完成后已清空测试提交记录。

### Git 提交

- 本地提交：`d9d1eab Add sample problem restore action`
- 已推送到远程仓库：`https://github.com/congxxx/CodingWeb.git`
- `git status --short --branch` 显示：`## main...origin/main`

## 11. 优化页面布局和运行结果切换

### 本次目标

修复右侧题目管理无法收起、三栏滚动时底线不齐，以及运行样例结果一次展示过多的问题。

### 已完成

- 顶部新增“题目管理”按钮，用来展开或收起右侧管理面板。
- 右侧题目管理面板内部新增“收起”按钮。
- 保存题目成功后，题目管理面板会自动收起，回到更专注的做题界面。
- 点击“新建题目”时，会自动展开题目管理面板，方便填写新题。
- 桌面端主布局改为固定视口高度，左侧题目列表、中间做题区、右侧管理区各自滚动，让三栏底线保持一致。
- 运行样例或提交后，结果区每次只展示一个用例结果。
- 多个用例结果通过“上一个”“下一个”按钮切换查看。

### 修改文件

- `public/index.html`
- `public/styles.css`
- `public/app.js`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check public/app.js` 通过。
- `node --check server.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`。
- `/api/problems` 可以正常返回题目列表。
- `data/db.json` 当前有本地运行记录变化，本次不会提交该文件。

## 12. 参考 LeetCode 优化题目区标签页

### 本次目标

参考 LeetCode 做题页的信息组织方式，把提交记录从代码运行区下方移动到题目内容区顶部，通过标签页切换查看。

### 参考结论

- LeetCode 题目页在题目内容区顶部提供多个标签页，例如 Description、Solution、Submissions、Discuss。
- 提交后，结果会进入 Submissions 相关区域，方便在题目上下文里回看历史提交。
- 本项目当前只保留最必要的两个标签页：`题目描述` 和 `提交记录`。

### 已完成

- 在题目描述卡片顶部新增标签页。
- `题目描述` 标签展示原来的题目描述、输入说明、输出说明、数据范围和样例。
- `提交记录` 标签展示当前题目的运行样例记录和正式提交记录。
- 提交记录不再放在代码编辑器下方，代码区更专注于编写、运行和查看本次结果。
- 切换题目时，提交记录会按当前题目过滤。
- 更新 `README.md` 中的提交记录位置说明。

### 修改文件

- `public/index.html`
- `public/styles.css`
- `public/app.js`
- `README.md`
- `开发记录/2026-04-27.md`

### 验证结果

- `node --check public/app.js` 通过。
- `node --check server.js` 通过。
- 首页访问 `http://127.0.0.1:3000` 返回 `200`。
