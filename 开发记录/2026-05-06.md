# 2026-05-06 开发记录

## 1. 迁移到 Spring Boot 并整理学习文档

### 本次目标

把项目从原来的 Node 单文件后端迁移为 Spring Boot 项目，并让结构更适合学习和后续扩展。

### 已完成

- 新增 Spring Boot 启动入口和 `pom.xml` 构建文件。
- 将题目管理、提交记录、本地 JSON 存储、Java 判题逻辑迁入 `src/main/java`。
- 保留 `public/` 作为静态前端资源目录，并让根路径直接打开首页。
- 增加详细注释，帮助理解启动、接口、业务、判题和数据模型的作用。
- 重写 README，补充整体框架、依赖说明、启动方式和 API 概览。

### 修改文件

- `pom.xml`
- `src/main/java/com/codingweb/**`
- `src/main/resources/application.properties`
- `public/app.js`
- `README.md`

### 验证结果

- 当前环境未安装 `mvn`，因此没有执行完整 Maven 构建。
- 已做基础文件检查和内容核对。

## 2. 绘制项目结构理解图

### 本次目标

为 README 增加一张漫画风格的项目结构理解图，帮助快速理解请求流和数据流。

### 已完成

- 新增 `docs/project-structure-comic.svg`。
- 在 README 的“整体框架”附近插入图片引用。

### 修改文件

- `docs/project-structure-comic.svg`
- `README.md`

## 3. 清理旧版本无用文件

### 本次目标

删除旧 Node 版本已不再需要的入口文件和本地环境产物，让仓库更接近当前 Spring Boot 版本。

### 已完成

- 删除旧的 `server.js`。
- 删除旧的 `package.json`。
- 清理本地构建和 IDE 产物。

### 修改文件

- `server.js`
- `package.json`
- `.idea/`
- `target/`
- `tmp/`
- `.uploads/`

### 说明

旧文件已经不再作为当前项目入口，保留它们只会增加理解负担，所以这次一并清理。
