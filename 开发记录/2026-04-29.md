# 2026-04-29 开发记录

## 1. 参考 LeetCode 优化前端做题界面

### 本次目标

让刷题页面更接近 LeetCode 的使用体验，并根据浏览器标注反馈继续调整细节。

### 已完成

- 将页面视觉调整为更接近 LeetCode 的浅色、橙色主按钮风格。
- 缩薄左侧题目卡片，移动端题目列表改为正常高度并支持内部滚动。
- 隐藏题目顶部大信息条，把时间和内存限制放到代码区标题栏。
- 将代码编辑区改为 Java 风格高亮显示。
- 在代码区和运行结果区之间增加可拖拽分隔条，方便调整上下区域大小。

### 修改文件

- `public/index.html`
- `public/styles.css`
- `public/app.js`

### 验证结果

- `node --check public/app.js` 通过。
- `node --check server.js` 通过。
- 本地首页访问 `http://127.0.0.1:3000/` 返回 `200`。

### Git 提交

- 本地提交：`a1319bd Refine LeetCode-style problem UI`
- 已推送到远程仓库：`https://github.com/congxxx/CodingWeb.git`

## 2. 修正样例运行记录并支持 Markdown/公式

### 本次目标

修正“运行样例”误写入提交记录的问题，并让题目描述、题目解析支持 Markdown 和公式展示。

### 已完成

- 将“运行样例”和“正式提交”拆分处理，样例运行不再写入提交记录。
- 提交记录列表只展示正式提交结果。
- 题目描述和题目解析支持 Markdown 渲染。
- 题目描述和题目解析支持 `\( \)`、`\[ \]` 公式渲染。
- 题目管理表单新增“题目解析”字段。

### 修改文件

- `server.js`
- `public/index.html`
- `public/app.js`
- `public/styles.css`

### 验证结果

- `node --check server.js` 通过。
- `node --check public/app.js` 通过。
- 本地提交并推送：`daac722 Support markdown problems and separate sample runs`

### 后续建议

- 可以继续补充 Markdown 实时预览。
- 如果需要完全离线公式渲染，可以考虑把 MathJax 改成本地静态资源。
